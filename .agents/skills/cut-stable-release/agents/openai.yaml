interface:
  display_name: 'Cut Stable Release'
  short_description: 'Finalize stable from master or an RC cutoff'
  default_prompt: 'Use $cut-stable-release to finalize a coco stable release from master or a selected RC cutoff, validate it, commit it, tag it, and push the source branch plus tag.'

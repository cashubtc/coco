---
'@cashu/coco-sqlite': patch
'@cashu/coco-sqlite-bun': patch
'@cashu/coco-expo-sqlite': patch
---

Move SQLite schema and migrations into the shared SQL storage package while keeping adapter schema exports compatible.

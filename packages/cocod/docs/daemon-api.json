{
  "name": "cocod-daemon-api",
  "version": "0.0.17",
  "transport": {
    "protocol": "http",
    "network": "tcp",
    "defaultUrl": "http://127.0.0.1:62626",
    "listenHostEnv": "COCOD_LISTEN_HOST",
    "listenPortEnv": "COCOD_LISTEN_PORT",
    "clientUrlEnv": "COCOD_URL"
  },
  "authentication": {
    "scheme": "bearer",
    "publicPaths": ["/health"],
    "protectedPaths": "all legacy routes and every /v1 route",
    "structuredLifecycleDescription": "lifecycle-api-v1.json"
  },
  "responseContract": {
    "scope": "remaining operational legacy routes",
    "success": {
      "shape": {
        "output": "unknown"
      }
    },
    "error": {
      "shape": {
        "error": "string"
      }
    }
  },
  "endpoints": [
    {
      "path": "/send/bolt11",
      "method": "POST",
      "state": "cocoSession.running",
      "body": {
        "invoice": "string",
        "mintUrl": "string (optional)"
      },
      "responses": {
        "200": "{ output: string }",
        "500": "{ error: string }"
      }
    },
    {
      "path": "/x-cashu/parse",
      "method": "POST",
      "state": "cocoSession.running",
      "body": {
        "request": "string"
      },
      "notes": "Accepts creqA and creqB. NUT-10 spending conditions are resolved by Coco during parse.",
      "responses": {
        "200": "{ output: string }",
        "400": "{ error: string }",
        "500": "{ error: string }"
      }
    },
    {
      "path": "/x-cashu/handle",
      "method": "POST",
      "state": "cocoSession.running",
      "body": {
        "request": "string",
        "mintUrl": "string (optional)"
      },
      "notes": "Accepts creqA and creqB. Coco enforces NUT-10 spending conditions: P2PK-locked requests are paid with locked outputs; unsupported or malformed conditions return 400 before proofs are prepared.",
      "responses": {
        "200": "{ output: string }",
        "400": "{ error: string }",
        "500": "{ error: string }"
      }
    },
    {
      "path": "/history",
      "method": "GET",
      "state": "cocoSession.running",
      "query": {
        "offset": "number (optional, default 0)",
        "limit": "number (optional, default 20, max 100)"
      },
      "responses": {
        "200": "{ output: unknown[] }",
        "400": "{ error: string }"
      }
    },
    {
      "path": "/events",
      "method": "GET",
      "state": "cocoSession.running",
      "responses": {
        "200": "text/event-stream"
      }
    },
    {
      "path": "/npc/address",
      "method": "GET",
      "state": "cocoSession.running",
      "responses": {
        "200": "{ output: string }",
        "500": "{ error: string }"
      }
    },
    {
      "path": "/npc/username",
      "method": "POST",
      "state": "cocoSession.running",
      "body": {
        "username": "string",
        "confirm": "boolean (optional)"
      },
      "responses": {
        "200": "{ output: unknown }",
        "400": "{ error: string }",
        "402": "{ error: string }",
        "500": "{ error: string }"
      }
    }
  ]
}

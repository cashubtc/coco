meta {
  name: Create BOLT11 Mint Quote
  type: http
  seq: 1
  tags: [
    mutation
    financial
  ]
}

post {
  url: {{baseUrl}}/v1/quotes/mint
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  ~Idempotency-Key: {{$guid}}
}

body:json {
  {
    "mintUrl": "{{mintUrl}}",
    "method": "bolt11",
    "amount": "{{amount}}",
    "unit": "{{unit}}"
  }
}

script:post-response {
  if (res.status === 201) bru.setVar('mintQuoteId', res.body.quoteId);
}

docs {
  Create a BOLT11 Mint Quote. This does not create a Mint Operation.

  Implemented v1 contract. Expected success status: 201.
}
